<% provide(:title, @title) %>

<% if logged_in? %>
    <div class = "row">
    	<aside class=" col-md-4">
    		<section class="user_infor">
    			<%= render "shared/user_info"%>
    		</section>
            <section class="stats">
                <%= render "shared/stats" %>
            </section>
    		<section class="micropost_form">
    			<%= render "shared/micropost_form" %>
    		</section>
    	</aside>
    	<div class="col-md-8">
    		<h3>Micropost Feed</h3>
    	    <%=render "shared/feed" %>
    	</div>
    </div>
<% else %>
	<%= render "shared/sign_up_button" %>
	<%= link_to image_tag("rails.png", alt: "rails logo"),'http://rubyonrails.org/' %>

<% end %>
<!--
<%= link_to image_tag("kitten.jpg", border: 0, class: 'kitten' ), {action: 'home', controller: 'static_pages'}, {class: 'kitten'} %>

-->
